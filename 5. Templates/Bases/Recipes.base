filters:
  and:
    - categories.contains(link("Recipes"))
    - '!file.name.contains("Template")'
views:
  - type: table
    name: Table
    groupBy:
      property: type
      direction: ASC
    order:
      - file.name
      - type
      - cuisine
      - rating
    sort:
      - property: file.name
        direction: DESC
    columnSize:
      file.name: 262
      note.type: 190
      note.cuisine: 161
